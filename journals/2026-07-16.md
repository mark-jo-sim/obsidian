
## Calendar

| Start | End | Title | Location |
| --- | --- | --- | --- |
| All day | All day | Home |  |
| 13:00 | 14:00 | Weekly Engineering Design Review |  |
| All day | All day | Home |  |


## Tasks

```tw
list
+next
```

## Prepare

### Context

- [[spanish_asr_sampling]]
- [[english_asr_resampling]]

### Workspace

#### Multivendor ASR

- Need to infer role per channel. Use existing code.
- `BUG`: unknown role map being returned, which contradicts prior findings!

##### Unknown role map error

- Customer: `brinks`
- Conversation id: `019e7da8-3761-7a1b-9245-c67314c76a22`
- Diagnosis: A previous function renaming columns accidentally overrode the speaker role column with the message name.
- Solution: fixed functions so that columns were not overwritten 

##### Cartesia API error

- Problem: on attempting to upload a file for transcription to the Cartesia API, get an unknown error returned 
- Diagnosis: splicing sub-segments using array slices rather than the `AudioSegment.get_sample_slice()` function resulted in null arrays
- Solution: use `AudioSegment.get_sample_slice()`

#### English message timestamps

- need to check if speaker role is also mapped to visitor channel 
- once that's done, assuming my assumption is correct, I can:
	1. Force align message timestamps by just turning the secondary role annotations to a tag that follows the visitor tag.
	2. Map each word to the nearest hit or substitution and get its timestamp.
-  a small blocker is the fact that I accidentally saved some JSON arrays as JSONL files in the transcripts subdirectories. Let's go ahead and normalize all of those to JSONL to make our lives easier. Also check where that JSONL was originally written. That way we can nip it at the source as well 

### EOD goals

#### Work environment

- [ ] OpenAI project issue
- [x] Download Spanish FLEURS data set to have data to play with locally 

#### Spanish ASR

- [x] Script for transcribing Spanish audio based on audio chunks from Deepgram message timestamps

#### English ASR

- [ ] Investigate `[SPEAKER:]` tags

## Review

### Progress

- [x]  Spanish vendor transcription script 

### Blockers

- [ ] English transcript to JSON formatting issue

### Plan

- [ ] item